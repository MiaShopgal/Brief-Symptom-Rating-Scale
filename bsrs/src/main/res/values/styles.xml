<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <!--Toolbar color-->
        <item name="colorPrimary">@color/title_red</item>
        <!--Status bar color-->
        <!--<item name="colorPrimaryDark">@color/accent_material_light</item>-->
        <!--Window color-->
        <item name="android:windowBackground">@color/pink_orange</item>

        <!--button pressed style-->
        <item name="android:buttonStyle">@style/pressed_button</item>
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar" parent="@style/Theme.AppCompat.Light">
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="pressed_button" parent="@android:style/Widget.Button">
        <item name="android:background">@drawable/button_clicked_blue</item>
    </style>
</resources>
